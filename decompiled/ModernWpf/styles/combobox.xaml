<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:ModernWpf.Controls" xmlns:primitives="clr-namespace:ModernWpf.Controls.Primitives" xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="TextBox.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <Thickness x:Key="ComboBoxTopHeaderMargin">0,0,0,4</Thickness>
    <Thickness x:Key="ComboBoxPadding">12,6,0,6</Thickness>
    <Thickness x:Key="ComboBoxEditableTextPadding">11,5,32,6</Thickness>
    <Double x:Key="ComboBoxMinHeight">32</Double>
    <Style x:Key="DefaultComboBoxItemStyle" TargetType="{x:Type ComboBoxItem}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Foreground" Value="{DynamicResource ComboBoxItemForeground}" />
        <Setter Property="Background" Value="{DynamicResource ComboBoxItemBackground}" />
        <Setter Property="TabNavigation" Value="Local" />
        <Setter Property="Padding" Value="{DynamicResource ComboBoxItemThemePadding}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static FocusVisualStyle}}" />
        <Setter Property="UseSystemFocusVisuals" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="False" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="LayoutRoot" Value="{DynamicResource ComboBoxItemBackgroundPointerOver}" Property="Background" />
                            <Setter TargetName="LayoutRoot" Value="{DynamicResource ComboBoxItemBorderBrushPointerOver}" Property="BorderBrush" />
                            <Setter TargetName="ContentPresenter" Value="{DynamicResource ComboBoxItemForegroundPointerOver}" Property="Foreground" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="False" />
                                <Condition Property="IsEnabled" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="LayoutRoot" Value="{DynamicResource ComboBoxItemBackgroundDisabled}" Property="Background" />
                            <Setter TargetName="LayoutRoot" Value="{DynamicResource ComboBoxItemBorderBrushDisabled}" Property="BorderBrush" />
                            <Setter TargetName="ContentPresenter" Value="{DynamicResource ComboBoxItemForegroundDisabled}" Property="Foreground" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True" />
                                <Condition Property="IsFocused" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="LayoutRoot" Value="{DynamicResource ComboBoxItemBackgroundSelected}" Property="Background" />
                            <Setter TargetName="LayoutRoot" Value="{DynamicResource ComboBoxItemBorderBrushSelected}" Property="BorderBrush" />
                            <Setter TargetName="ContentPresenter" Value="{DynamicResource ComboBoxItemForegroundSelected}" Property="Foreground" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True" />
                                <Condition Property="IsFocused" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="LayoutRoot" Value="{DynamicResource ComboBoxItemBackgroundSelectedUnfocused}" Property="Background" />
                            <Setter TargetName="LayoutRoot" Value="{DynamicResource ComboBoxItemBorderBrushSelectedUnfocused}" Property="BorderBrush" />
                            <Setter TargetName="ContentPresenter" Value="{DynamicResource ComboBoxItemForegroundSelectedUnfocused}" Property="Foreground" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True" />
                                <Condition Property="IsEnabled" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="LayoutRoot" Value="{DynamicResource ComboBoxItemBackgroundSelectedDisabled}" Property="Background" />
                            <Setter TargetName="LayoutRoot" Value="{DynamicResource ComboBoxItemBorderBrushSelectedDisabled}" Property="BorderBrush" />
                            <Setter TargetName="ContentPresenter" Value="{DynamicResource ComboBoxItemForegroundSelectedDisabled}" Property="Foreground" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="LayoutRoot" Value="{DynamicResource ComboBoxItemBackgroundSelectedPointerOver}" Property="Background" />
                            <Setter TargetName="LayoutRoot" Value="{DynamicResource ComboBoxItemBorderBrushSelectedPointerOver}" Property="BorderBrush" />
                            <Setter TargetName="ContentPresenter" Value="{DynamicResource ComboBoxItemForegroundSelectedPointerOver}" Property="Foreground" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                    <Border Name="LayoutRoot" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="True">
                        <ContentPresenter Name="ContentPresenter" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Margin="{TemplateBinding Padding}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="{x:Type ComboBoxItem}" TargetType="{x:Type ComboBoxItem}" BasedOn="{StaticResource DefaultComboBoxItemStyle}" />
    <Style x:Key="ComboBoxToggleButton" TargetType="{x:Type ToggleButton}">
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="ClickMode" Value="Release" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Grid Background="{TemplateBinding Background}" SnapsToDevicePixels="True" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="ComboBoxTextBoxStyle" TargetType="{x:Type TextBox}" BasedOn="{StaticResource DefaultComboBoxItemStyle}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="BorderElement" Value="{DynamicResource TextControlBackgroundDisabled}" Property="Background" />
                            <Setter TargetName="BorderElement" Value="{DynamicResource TextControlBorderBrushDisabled}" Property="BorderBrush" />
                            <Setter TargetName="PART_ContentHost" Value="{DynamicResource TextControlForegroundDisabled}" Property="Foreground" />
                            <Setter TargetName="PlaceholderTextContentPresenter" Value="{DynamicResource TextControlPlaceholderForegroundDisabled}" Property="Foreground" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BorderElement" Value="{DynamicResource TextControlBackgroundPointerOver}" Property="Background" />
                            <Setter TargetName="BorderElement" Value="{DynamicResource TextControlBorderBrushPointerOver}" Property="BorderBrush" />
                            <Setter TargetName="PART_ContentHost" Value="{DynamicResource TextControlForegroundPointerOver}" Property="Foreground" />
                            <Setter TargetName="PlaceholderTextContentPresenter" Value="{DynamicResource TextControlPlaceholderForegroundPointerOver}" Property="Foreground" />
                        </Trigger>
                        <Trigger Property="IsSelectionActive" Value="True">
                            <Setter TargetName="BorderElement" Value="{DynamicResource TextControlBackgroundFocused}" Property="Background" />
                            <Setter TargetName="BorderElement" Value="{DynamicResource TextControlBorderBrushFocused}" Property="BorderBrush" />
                            <Setter TargetName="BorderElement" Value="{DynamicResource TextControlBorderThemeThicknessFocused}" Property="BorderThickness" />
                            <Setter Value="{DynamicResource TextControlForegroundFocused}" Property="Foreground" />
                            <Setter Value="{DynamicResource TextControlForegroundFocused}" Property="CaretBrush" />
                            <Setter TargetName="PlaceholderTextContentPresenter" Value="{DynamicResource TextControlPlaceholderForegroundFocused}" Property="Foreground" />
                        </Trigger>
                        <Trigger Property="Text" Value="">
                            <Setter TargetName="PlaceholderTextContentPresenter" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                    <Grid SnapsToDevicePixels="True">
                        <Border Name="BorderElement" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{TemplateBinding CornerRadius}" />
                        <ScrollViewer Name="PART_ContentHost" Style="{DynamicResource TextControlContentHostStyle}" Margin="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" HorizontalScrollBarVisibility="{TemplateBinding HorizontalScrollBarVisibility}" VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}" IsDeferredScrollingEnabled="{TemplateBinding IsDeferredScrollingEnabled}" IsTabStop="False" />
                        <TextBlock Name="PlaceholderTextContentPresenter" Foreground="{DynamicResource TextControlPlaceholderForeground}" Margin="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" Text="{TemplateBinding PlaceholderText}" TextAlignment="{TemplateBinding TextAlignment}" TextWrapping="{TemplateBinding TextWrapping}" IsHitTestVisible="False" Visibility="Collapsed" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="DefaultComboBoxStyle" TargetType="{x:Type ComboBox}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Padding" Value="{DynamicResource ComboBoxPadding}" />
        <Setter Property="MaxDropDownHeight" Value="504" />
        <Setter Property="Foreground" Value="{DynamicResource ComboBoxForeground}" />
        <Setter Property="Background" Value="{DynamicResource ComboBoxBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ComboBoxBorderBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource ComboBoxBorderThemeThickness}" />
        <Setter Property="TabNavigation" Value="Once" />
        <Setter Property="TextBoxStyle" Value="{StaticResource DefaultComboBoxItemStyle}" />
        <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="IsDeferredScrollingEnabled" Value="False" />
        <Setter Property="CanContentScroll" Value="true" />
        <Setter Property="PanningMode" Value="Both" />
        <Setter Property="IsFlicksEnabled" Value="False" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Top" />
        <Setter Property="FontFamily" Value="{DynamicResource ContentControlThemeFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
        <Setter Property="PlaceholderForeground" Value="{DynamicResource ComboBoxPlaceHolderForeground}" />
        <Setter Property="UseSystemFocusVisuals" Value="{DynamicResource IsApplicationFocusVisualKindReveal}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static FocusVisualStyle}}" />
        <Setter Property="KeepInteriorCornersSquare" Value="True" />
        <Setter Property="CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
        <Setter Property="ErrorTemplate" Value="{DynamicResource TextControlValidationErrorTemplate}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <ControlTemplate.Resources>
                        <StreamGeometry RelativeSource="ChevronDown">M 18.935547 4.560547 L 19.814453 5.439453 L 10 15.253906 L 0.185547 5.439453 L 1.064453 4.560547 L 10 13.496094 Z</StreamGeometry>
                    </ControlTemplate.Resources>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Background" Value="{DynamicResource ComboBoxBackgroundPointerOver}" Property="Background" />
                            <Setter TargetName="Background" Value="{DynamicResource ComboBoxBorderBrushPointerOver}" Property="BorderBrush" />
                            <Setter TargetName="ContentPresenter" Value="{DynamicResource ComboBoxForegroundPointerOver}" Property="Foreground" />
                            <Setter TargetName="PlaceholderTextBlock" Value="{DynamicResource ComboBoxPlaceHolderForegroundPointerOver}" Property="Foreground" />
                        </Trigger>
                        <Trigger SourceName="ToggleButton" Property="IsPressed" Value="True">
                            <Setter TargetName="Background" Value="{DynamicResource ComboBoxBackgroundPressed}" Property="Background" />
                            <Setter TargetName="Background" Value="{DynamicResource ComboBoxBorderBrushPressed}" Property="BorderBrush" />
                            <Setter TargetName="ContentPresenter" Value="{DynamicResource ComboBoxForegroundPressed}" Property="Foreground" />
                            <Setter TargetName="PlaceholderTextBlock" Value="{DynamicResource ComboBoxPlaceHolderForegroundPressed}" Property="Foreground" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Background" Value="{DynamicResource ComboBoxBackgroundDisabled}" Property="Background" />
                            <Setter TargetName="Background" Value="{DynamicResource ComboBoxBorderBrushDisabled}" Property="BorderBrush" />
                            <Setter TargetName="HeaderContentPresenter" Value="{DynamicResource ComboBoxHeaderForegroundDisabled}" Property="Foreground" />
                            <Setter TargetName="ContentPresenter" Value="{DynamicResource ComboBoxForegroundDisabled}" Property="Foreground" />
                            <Setter TargetName="PlaceholderTextBlock" Value="{DynamicResource ComboBoxPlaceHolderForegroundDisabled}" Property="Foreground" />
                            <Setter TargetName="DropDownGlyph" Value="{DynamicResource ComboBoxDropDownGlyphForegroundDisabled}" Property="Foreground" />
                        </Trigger>
                        <Trigger Property="ShowFocusVisual" Value="True">
                            <Setter TargetName="HighlightBackground" Property="Opacity" Value="1" />
                            <Setter TargetName="HighlightBackground" Value="{DynamicResource ComboBoxBackgroundFocused}" Property="Background" />
                            <Setter TargetName="HighlightBackground" Value="{DynamicResource ComboBoxBackgroundBorderBrushFocused}" Property="BorderBrush" />
                            <Setter TargetName="ContentPresenter" Value="{DynamicResource ComboBoxForegroundFocused}" Property="Foreground" />
                            <Setter TargetName="PlaceholderTextBlock" Value="{DynamicResource ComboBoxPlaceHolderForegroundFocused}" Property="Foreground" />
                            <Setter TargetName="DropDownGlyph" Value="{DynamicResource ComboBoxDropDownGlyphForegroundFocused}" Property="Foreground" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="ShowFocusVisual" Value="True" />
                                <Condition SourceName="ToggleButton" Property="IsPressed" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="HighlightBackground" Property="Opacity" Value="1" />
                            <Setter TargetName="ContentPresenter" Value="{DynamicResource ComboBoxForegroundFocusedPressed}" Property="Foreground" />
                            <Setter TargetName="PlaceholderTextBlock" Value="{DynamicResource ComboBoxPlaceHolderForegroundFocusedPressed}" Property="Foreground" />
                            <Setter TargetName="DropDownGlyph" Value="{DynamicResource ComboBoxDropDownGlyphForegroundFocusedPressed}" Property="Foreground" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="ShowFocusVisual" Value="True" />
                                <Condition Property="IsDropDownOpen" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PopupBorder" Property="Visibility" Value="Visible" />
                        </MultiTrigger>
                        <Trigger Property="IsEditable" Value="true">
                            <Setter TargetName="ContentPresenter" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="ToggleButton" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                            <Setter TargetName="DropDownOverlay" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEditable" Value="False" />
                                <Condition Property="SelectedIndex" Value="-1" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PlaceholderTextBlock" Property="Visibility" Value="Visible" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition SourceName="PART_EditableTextBox" Property="IsSelectionActive" Value="True" />
                                <Condition SourceName="DropDownOverlay" Property="IsMouseOver" Value="False" />
                                <Condition SourceName="DropDownOverlay" Property="IsPressed" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="DropDownGlyph" Value="{DynamicResource ComboBoxEditableDropDownGlyphForeground}" Property="Foreground" />
                            <Setter TargetName="DropDownOverlay" Property="Margin" Value="0,2,2,2" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition SourceName="PART_EditableTextBox" Property="IsSelectionActive" Value="True" />
                                <Condition SourceName="DropDownOverlay" Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="DropDownGlyph" Value="{DynamicResource ComboBoxEditableDropDownGlyphForeground}" Property="Foreground" />
                            <Setter TargetName="DropDownOverlay" Value="{DynamicResource ComboBoxFocusedDropDownBackgroundPointerOver}" Property="Background" />
                            <Setter TargetName="DropDownOverlay" Property="Margin" Value="0,2,2,2" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition SourceName="PART_EditableTextBox" Property="IsSelectionActive" Value="True" />
                                <Condition SourceName="DropDownOverlay" Property="IsPressed" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="DropDownGlyph" Value="{DynamicResource ComboBoxEditableDropDownGlyphForeground}" Property="Foreground" />
                            <Setter TargetName="DropDownOverlay" Value="{DynamicResource ComboBoxFocusedDropDownBackgroundPointerPressed}" Property="Background" />
                            <Setter TargetName="DropDownOverlay" Property="Margin" Value="0,2,2,2" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition SourceName="PART_EditableTextBox" Property="IsSelectionActive" Value="False" />
                                <Condition SourceName="DropDownOverlay" Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="DropDownOverlay" Value="{DynamicResource ComboBoxDropDownBackgroundPointerOver}" Property="Background" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition SourceName="PART_EditableTextBox" Property="IsSelectionActive" Value="False" />
                                <Condition SourceName="DropDownOverlay" Property="IsPressed" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="DropDownOverlay" Value="{DynamicResource ComboBoxDropDownBackgroundPointerPressed}" Property="Background" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsGrouping" Value="true" />
                                <Condition Property="IsVirtualizingWhenGrouping" Value="false" />
                            </MultiTrigger.Conditions>
                            <Setter Property="CanContentScroll" Value="false" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                    <Grid Name="LayoutRoot" SnapsToDevicePixels="True">
                        <Grid.Resources>
                            <Storyboard RelativeSource="OverlayOpeningAnimation">
                                <DoubleAnimationUsingKeyFrames TargetName="Shdw" TargetProperty="Opacity">
                                    <DiscreteDoubleKeyFrame KeyTime="0:0:0" Value="0.0" />
                                    <SplineDoubleKeyFrame KeyTime="0:0:0.383" KeySpline="0.1,0.9 0.2,1.0" Value="1.0" />
                                </DoubleAnimationUsingKeyFrames>
                            </Storyboard>
                        </Grid.Resources>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="32" />
                        </Grid.ColumnDefinitions>
                        <local:ContentPresenterEx Name="HeaderContentPresenter" Row="0" Column="0" ColumnSpan="2" Content="{TemplateBinding Header}" ContentTemplate="{TemplateBinding HeaderTemplate}" FlowDirection="{TemplateBinding FlowDirection}" Foreground="{DynamicResource ComboBoxHeaderForeground}" FontWeight="{DynamicResource ComboBoxHeaderThemeFontWeight}" Margin="{DynamicResource ComboBoxTopHeaderMargin}" TextWrapping="Wrap" VerticalAlignment="Top" Visibility="{TemplateBinding HeaderVisibility}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        <Border Name="Background" Row="1" Column="0" ColumnSpan="2" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{TemplateBinding CornerRadius}" MinWidth="{DynamicResource ComboBoxThemeMinWidth}" IsTemplateValidationAdornerSite="True" />
                        <Border Name="HighlightBackground" Row="1" Column="0" ColumnSpan="2" Background="{DynamicResource ComboBoxBackgroundUnfocused}" BorderBrush="{DynamicResource ComboBoxBackgroundBorderBrushUnfocused}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{TemplateBinding CornerRadius}" Opacity="0" />
                        <ContentPresenter Name="ContentPresenter" Row="1" Column="0" Margin="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        <TextBlock Name="PlaceholderTextBlock" Row="1" Column="0" Margin="{TemplateBinding Padding}" Padding="0,0,32,0" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Text="{TemplateBinding PlaceholderText}" Foreground="{TemplateBinding PlaceholderForeground}" IsHitTestVisible="False" Visibility="Collapsed" />
                        <ToggleButton Name="ToggleButton" Row="1" Column="0" ColumnSpan="2" Style="{StaticResource DefaultTextBoxStyle}" Background="Transparent" IsChecked="{Binding IsDropDownOpen , Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" />
                        <TextBox Name="PART_EditableTextBox" Row="1" Column="0" ColumnSpan="2" IsReadOnly="{TemplateBinding IsReadOnly}" Style="{TemplateBinding TextBoxStyle}" Margin="0,0,0,0" Padding="{DynamicResource ComboBoxEditableTextPadding}" BorderBrush="Transparent" HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" PlaceholderText="{TemplateBinding PlaceholderText}" PlaceholderForeground="{TemplateBinding PlaceholderForeground}" Visibility="Collapsed" Name="{TemplateBinding Name}" CornerRadius="{TemplateBinding CornerRadius}" />
                        <ToggleButton Name="DropDownOverlay" Style="{StaticResource ComboBoxTextBoxStyle}" Row="1" Column="1" Background="Transparent" Margin="0,1,1,1" Visibility="Collapsed" Width="30" HorizontalAlignment="Right" IsChecked="{Binding IsDropDownOpen , Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" />
                        <local:FontIconFallback Name="DropDownGlyph" MinHeight="{DynamicResource ComboBoxMinHeight}" Row="1" Column="1" IsHitTestVisible="False" Margin="0,0,10,0" Foreground="{DynamicResource ComboBoxDropDownGlyphForeground}" FontFamily="{DynamicResource SymbolThemeFontFamily}" FontSize="12" Data="{StaticResource ComboBoxToggleButton}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                        <ContentPresenter Name="DescriptionPresenter" Row="2" Column="0" ColumnSpan="2" Foreground="{DynamicResource SystemControlDescriptionTextForegroundBrush}" Content="{TemplateBinding Description}" Visibility="{TemplateBinding DescriptionVisibility}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        <Popup Name="PART_Popup" AllowsTransparency="True" Placement="Bottom" PopupAnimation="None" PlacementTarget="{Binding ElementName=Background}" IsOpen="{Binding IsDropDownOpen , RelativeSource={RelativeSource TemplatedParent}}">
                            <Popup.PlacementRectangle>
                                <MultiBinding Converter="{primitives:PlacementRectangleConverter Margin=0,1,0,1}">
                                    <Binding ElementName="Background" Path="ActualWidth" />
                                    <Binding ElementName="Background" Path="ActualHeight" />
                                </MultiBinding>
                            </Popup.PlacementRectangle>
                            <primitives:ThemeShadowChrome Name="Shdw" MaxHeight="{TemplateBinding MaxDropDownHeight}" IsShadowEnabled="{DynamicResource {x:Static DropShadow}}" Storyboard="{StaticResource ComboBoxToggleButton}" MinWidth="{Binding ActualWidth , ElementName=LayoutRoot}" CornerRadius="{Binding ElementName=PopupBorder, Path=CornerRadius}">
                                <Border Name="PopupBorder" Background="{DynamicResource ComboBoxDropDownBackground}" HorizontalAlignment="Stretch" CornerRadius="{DynamicResource OverlayCornerRadius}">
                                    <Border BorderBrush="{DynamicResource ComboBoxDropDownBorderBrush}" BorderThickness="{DynamicResource ComboBoxDropdownBorderThickness}" Padding="{DynamicResource ComboBoxDropdownBorderPadding}" CornerRadius="{Binding ElementName=PopupBorder, Path=CornerRadius}">
                                        <ScrollViewer Name="ScrollViewer" Foreground="{DynamicResource ComboBoxDropDownForeground}" HorizontalScrollBarVisibility="{TemplateBinding HorizontalScrollBarVisibility}" VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}" IsDeferredScrollingEnabled="{TemplateBinding IsDeferredScrollingEnabled}">
                                            <ItemsPresenter Margin="{DynamicResource ComboBoxDropdownContentMargin}" DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                        </ScrollViewer>
                                    </Border>
                                </Border>
                            </primitives:ThemeShadowChrome>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="{x:Type ComboBox}" TargetType="{x:Type ComboBox}" BasedOn="{StaticResource DefaultComboBoxItemStyle}" />
    <Style x:Key="DataGridComboBoxStyle" TargetType="{x:Type ComboBox}" BasedOn="{StaticResource DefaultComboBoxItemStyle}">
        <Setter Property="Padding" Value="12,0,0,0" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="CornerRadius" Value="0" />
        <Setter Property="ErrorTemplate" Value="{DynamicResource DataGridTextControlValidationErrorTemplate}" />
    </Style>
    <Style x:Key="DataGridTextBlockComboBoxStyle" TargetType="{x:Type ComboBox}" BasedOn="{StaticResource DefaultComboBoxItemStyle}">
        <Setter Property="Padding" Value="12,0,0,0" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <ContentPresenter Margin="{TemplateBinding Padding}" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" IsHitTestVisible="false" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>

