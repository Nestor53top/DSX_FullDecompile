<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:root="clr-namespace:ModernWpf" xmlns:local="clr-namespace:ModernWpf.Controls" xmlns:primitives="clr-namespace:ModernWpf.Controls.Primitives">
    <Style x:Key="TitleBarBackButtonStyle" TargetType="{x:Type primitives:TitleBarButton}">
        <Setter Property="Background" Value="{DynamicResource AccentButtonBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource AccentButtonForeground}" />
        <Setter Property="HoverBackground" Value="{DynamicResource AccentButtonBackgroundPointerOver}" />
        <Setter Property="HoverForeground" Value="{DynamicResource AccentButtonForegroundPointerOver}" />
        <Setter Property="PressedBackground" Value="{DynamicResource AccentButtonBackgroundPressed}" />
        <Setter Property="PressedForeground" Value="{DynamicResource AccentButtonForegroundPressed}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Width" Value="48" />
        <Setter Property="Content" Value="M 20 10 L 3.828125 10 L 10.410156 16.582031 L 9 18 L 0 9 L 9 0 L 10.410156 1.416016 L 3.828125 8 L 20 8 Z" />
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <local:FontIconFallback Data="{Binding}" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <WindowChrome x:Key="DefaultWindowChrome" CaptionHeight="{DynamicResource {x:Static HeightKey}}" CornerRadius="0" GlassFrameThickness="0,1,0,0" UseAeroCaptionButtons="False" />
    <Style x:Key="DefaultWindowStyle" TargetType="{x:Type Window}">
        <Style.Triggers>
            <Trigger Property="WindowChrome" Value="{Null}">
                <Setter Property="FixMaximizedWindow" Value="False" />
            </Trigger>
        </Style.Triggers>
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Foreground" Value="{DynamicResource SystemControlPageTextBaseHighBrush}" />
        <Setter Property="Background" Value="{DynamicResource SystemControlPageBackgroundAltHighBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource WindowBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontFamily" Value="{DynamicResource ContentControlThemeFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="WindowChrome" Value="{DynamicResource DefaultWindowChrome}" />
        <Setter Property="IsThemeAware" Value="True" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource SystemControlForegroundBaseHighBrush}" />
        <Setter Property="InactiveBackground" Value="Transparent" />
        <Setter Property="InactiveForeground" Value="{DynamicResource SystemControlDisabledBaseMediumLowBrush}" />
        <Setter Property="BackButtonStyle" Value="{StaticResource TitleBarBackButtonStyle}" />
        <Setter Property="FixMaximizedWindow" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Window}">
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsActive" Value="False">
                            <Setter TargetName="LayoutRoot" Value="{DynamicResource WindowBorderInactive}" Property="BorderBrush" />
                            <Setter TargetName="HighContrastBorder" Value="{DynamicResource {x:Static InactiveCaptionBrush}}" Property="BorderBrush" />
                        </Trigger>
                        <DataTrigger Value="true" Binding="{Binding Path=(SystemParameters.HighContrast)}">
                            <Setter TargetName="ContentGrid" Property="Margin" Value="7,0,7,7" />
                            <Setter TargetName="HighContrastBorder" Property="Visibility" Value="Visible" />
                        </DataTrigger>
                        <Trigger Property="WindowState" Value="Maximized">
                            <Setter TargetName="LayoutRoot" Property="BorderThickness" Value="0" />
                            <Setter TargetName="ContentGrid" Property="Margin" Value="0" />
                            <Setter TargetName="HighContrastBorder" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="ExtendViewIntoTitleBar" Value="True">
                            <Setter TargetName="AdornerDecorator" Property="Row" Value="0" />
                            <Setter TargetName="AdornerDecorator" Property="RowSpan" Value="2" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="ResizeMode" Value="CanResizeWithGrip" />
                                <Condition Property="WindowState" Value="Normal" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="WindowResizeGrip" Property="Visibility" Value="Visible" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                    <Border Name="LayoutRoot" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                        <Grid>
                            <Grid Name="ContentGrid">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition />
                                </Grid.RowDefinitions>
                                <primitives:TitleBarControl Icon="{TemplateBinding Icon}" Title="{TemplateBinding Title}" IsActive="{TemplateBinding IsActive}" Background="{TemplateBinding Background}" Foreground="{TemplateBinding Foreground}" InactiveBackground="{TemplateBinding InactiveBackground}" InactiveForeground="{TemplateBinding InactiveForeground}" Style="{TemplateBinding Style}" ButtonStyle="{TemplateBinding ButtonStyle}" IsIconVisible="{TemplateBinding IsIconVisible}" IsBackButtonVisible="{TemplateBinding IsBackButtonVisible}" IsBackEnabled="{TemplateBinding IsBackEnabled}" BackButtonCommand="{TemplateBinding BackButtonCommand}" BackButtonCommandParameter="{TemplateBinding BackButtonCommandParameter}" BackButtonCommandTarget="{TemplateBinding BackButtonCommandTarget}" BackButtonStyle="{TemplateBinding BackButtonStyle}" ExtendViewIntoTitleBar="{TemplateBinding ExtendViewIntoTitleBar}" ZIndex="1" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                <AdornerDecorator Name="AdornerDecorator" Row="1">
                                    <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                </AdornerDecorator>
                                <ResizeGrip Name="WindowResizeGrip" ResizeGripDirection="BottomRight" Row="1" HorizontalAlignment="Right" VerticalAlignment="Bottom" IsTabStop="false" Visibility="Collapsed" />
                            </Grid>
                            <Border Name="HighContrastBorder" BorderBrush="{DynamicResource {x:Static ActiveCaptionBrush}}" BorderThickness="7,1,7,7" IsHitTestVisible="False" Visibility="Collapsed" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="{x:Type Window}" TargetType="{x:Type Window}" BasedOn="{StaticResource TitleBarBackButtonStyle}" />
</ResourceDictionary>

