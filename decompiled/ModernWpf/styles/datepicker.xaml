<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:ModernWpf.Controls" xmlns:primitives="clr-namespace:ModernWpf.Controls.Primitives">
    <Thickness x:Key="CalendarDatePickerTopHeaderMargin">0,0,0,4</Thickness>
    <Style x:Key="DefaultDatePickerTextBoxStyle" TargetType="{x:Type DatePickerTextBox}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Foreground" Value="{DynamicResource CalendarDatePickerTextForegroundSelected}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="PanningMode" Value="VerticalFirst" />
        <Setter Property="IsFlicksEnabled" Value="False" />
        <Setter Property="FocusVisualStyle" Value="{Null}" />
        <Setter Property="ContextMenu" Value="{DynamicResource TextControlContextMenu}" />
        <Setter Property="UsingTextContextMenu" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DatePickerTextBox}">
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="PART_ContentHost" Value="{DynamicResource CalendarDatePickerTextForegroundDisabled}" Property="Foreground" />
                            <Setter TargetName="PART_Watermark" Value="{DynamicResource CalendarDatePickerTextForegroundDisabled}" Property="Foreground" />
                        </Trigger>
                        <Trigger Property="IsSelectionActive" Value="True">
                            <Setter TargetName="BorderElement" Value="{DynamicResource TextControlBackgroundFocused}" Property="Background" />
                            <Setter TargetName="BorderElement" Value="{DynamicResource TextControlBorderBrushFocused}" Property="BorderBrush" />
                            <Setter TargetName="BorderElement" Value="{DynamicResource TextControlBorderThemeThicknessFocused}" Property="BorderThickness" />
                            <Setter Value="{DynamicResource TextControlForegroundFocused}" Property="Foreground" />
                            <Setter Value="{DynamicResource TextControlForegroundFocused}" Property="CaretBrush" />
                            <Setter TargetName="PART_Watermark" Value="{DynamicResource TextControlPlaceholderForegroundFocused}" Property="Foreground" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                    <Grid SnapsToDevicePixels="True">
                        <Grid.VisualStateGroups>
                            <VisualStateGroup x:Name="WatermarkStates">
                                <VisualState x:Name="Unwatermarked" />
                                <VisualState x:Name="Watermarked">
                                    <Storyboard>
                                        <DoubleAnimation TargetName="ContentElement" TargetProperty="Opacity" To="0" Duration="0" />
                                        <DoubleAnimation TargetName="PART_Watermark" TargetProperty="Opacity" To="1" Duration="0" />
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </Grid.VisualStateGroups>
                        <Border Name="BorderElement" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{TemplateBinding CornerRadius}" />
                        <Border Name="ContentElement" Padding="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        <ContentControl Name="PART_Watermark" Focusable="False" IsHitTestVisible="False" Opacity="0" Foreground="{DynamicResource CalendarDatePickerTextForeground}" Margin="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        <ScrollViewer Name="PART_ContentHost" Style="{DynamicResource TextControlContentHostStyle}" Margin="0" Padding="{TemplateBinding Padding}" HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="{x:Type DatePickerTextBox}" TargetType="{x:Type DatePickerTextBox}" BasedOn="{StaticResource DefaultDatePickerTextBoxStyle}" />
    <Style x:Key="DefaultDatePickerStyle" TargetType="{x:Type DatePicker}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Foreground" Value="{DynamicResource CalendarDatePickerForeground}" />
        <Setter Property="IsTodayHighlighted" Value="True" />
        <Setter Property="SelectedDateFormat" Value="Short" />
        <Setter Property="CalendarStyle" Value="{DynamicResource DatePickerCalendarStyle}" />
        <Setter Property="Background" Value="{DynamicResource CalendarDatePickerBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CalendarDatePickerBorderBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource CalendarDatePickerBorderThemeThickness}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
        <Setter Property="Padding" Value="{DynamicResource TextControlThemePadding}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static FocusVisualStyle}}" />
        <Setter Property="UseSystemFocusVisuals" Value="{DynamicResource IsApplicationFocusVisualKindReveal}" />
        <Setter Property="CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
        <Setter Property="IsEnabled" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DatePicker}">
                    <ControlTemplate.Resources>
                        <StreamGeometry Value="Calendar">F1 M 7.5 7.5 L 8.75 7.5 L 8.75 8.75 L 7.5 8.75 Z M 11.25 15 L 12.5 15 L 12.5 16.25 L 11.25 16.25 Z M 15 7.5 L 16.25 7.5 L 16.25 8.75 L 15 8.75 Z M 11.25 7.5 L 12.5 7.5 L 12.5 8.75 L 11.25 8.75 Z M 7.5 10 L 8.75 10 L 8.75 11.25 L 7.5 11.25 Z M 3.75 10 L 5 10 L 5 11.25 L 3.75 11.25 Z M 15 10 L 16.25 10 L 16.25 11.25 L 15 11.25 Z M 11.25 10 L 12.5 10 L 12.5 11.25 L 11.25 11.25 Z M 7.5 12.5 L 8.75 12.5 L 8.75 13.75 L 7.5 13.75 Z M 3.75 12.5 L 5 12.5 L 5 13.75 L 3.75 13.75 Z M 15 12.5 L 16.25 12.5 L 16.25 13.75 L 15 13.75 Z M 11.25 12.5 L 12.5 12.5 L 12.5 13.75 L 11.25 13.75 Z M 7.5 15 L 8.75 15 L 8.75 16.25 L 7.5 16.25 Z M 3.75 15 L 5 15 L 5 16.25 L 3.75 16.25 Z M 20 1.25 L 20 18.75 L 0 18.75 L 0 1.25 L 3.75 1.25 L 3.75 0 L 5 0 L 5 1.25 L 15 1.25 L 15 0 L 16.25 0 L 16.25 1.25 Z M 1.25 2.5 L 1.25 5 L 18.75 5 L 18.75 2.5 L 16.25 2.5 L 16.25 3.75 L 15 3.75 L 15 2.5 L 5 2.5 L 5 3.75 L 3.75 3.75 L 3.75 2.5 Z M 18.75 17.5 L 18.75 6.25 L 1.25 6.25 L 1.25 17.5 Z</StreamGeometry>
                    </ControlTemplate.Resources>
                    <ControlTemplate.Triggers>
                        <Trigger SourceName="PART_Root" Property="IsMouseOver" Value="True">
                            <Setter TargetName="Background" Value="{DynamicResource CalendarDatePickerBackgroundPointerOver}" Property="Background" />
                            <Setter TargetName="Background" Value="{DynamicResource CalendarDatePickerBorderBrushPointerOver}" Property="BorderBrush" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition SourceName="PART_TextBox" Property="IsSelectionActive" Value="True" />
                                <Condition SourceName="PART_Button" Property="IsMouseOver" Value="False" />
                                <Condition SourceName="PART_Button" Property="IsPressed" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_Button" Value="{DynamicResource ComboBoxEditableDropDownGlyphForeground}" Property="Foreground" />
                            <Setter TargetName="PART_Button" Property="Padding" Value="0,2,2,2" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition SourceName="PART_TextBox" Property="IsSelectionActive" Value="True" />
                                <Condition SourceName="PART_Button" Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_Button" Value="{DynamicResource ComboBoxEditableDropDownGlyphForeground}" Property="Foreground" />
                            <Setter TargetName="PART_Button" Value="{DynamicResource ComboBoxFocusedDropDownBackgroundPointerOver}" Property="Background" />
                            <Setter TargetName="PART_Button" Property="Padding" Value="0,2,2,2" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition SourceName="PART_TextBox" Property="IsSelectionActive" Value="True" />
                                <Condition SourceName="PART_Button" Property="IsPressed" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_Button" Value="{DynamicResource ComboBoxEditableDropDownGlyphForeground}" Property="Foreground" />
                            <Setter TargetName="PART_Button" Value="{DynamicResource ComboBoxFocusedDropDownBackgroundPointerPressed}" Property="Background" />
                            <Setter TargetName="PART_Button" Property="Padding" Value="0,2,2,2" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition SourceName="PART_TextBox" Property="IsSelectionActive" Value="False" />
                                <Condition SourceName="PART_Button" Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_Button" Value="{DynamicResource ComboBoxDropDownBackgroundPointerOver}" Property="Background" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition SourceName="PART_TextBox" Property="IsSelectionActive" Value="False" />
                                <Condition SourceName="PART_Button" Property="IsPressed" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_Button" Value="{DynamicResource ComboBoxDropDownBackgroundPointerPressed}" Property="Background" />
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Background" Value="{DynamicResource CalendarDatePickerBackgroundDisabled}" Property="Background" />
                            <Setter TargetName="Background" Value="{DynamicResource CalendarDatePickerBorderBrushDisabled}" Property="BorderBrush" />
                            <Setter TargetName="HeaderContentPresenter" Value="{DynamicResource CalendarDatePickerHeaderForegroundDisabled}" Property="Foreground" />
                            <Setter TargetName="PART_TextBox" Value="{DynamicResource CalendarDatePickerTextForegroundDisabled}" Property="Foreground" />
                            <Setter TargetName="PART_Button" Value="{DynamicResource CalendarDatePickerCalendarGlyphForegroundDisabled}" Property="Foreground" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                    <Grid SnapsToDevicePixels="True">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <local:ContentPresenterEx Name="HeaderContentPresenter" Visibility="{TemplateBinding HeaderVisibility}" Content="{TemplateBinding Header}" ContentTemplate="{TemplateBinding HeaderTemplate}" Margin="{StaticResource DefaultDatePickerTextBoxStyle}" TextWrapping="Wrap" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        <Grid Name="PART_Root" Row="1">
                            <Grid.Resources>
                                <ControlTemplate Value="DropDownButtonTemplate" TargetType="{x:Type Button}">
                                    <Grid>
                                        <Rectangle Fill="{TemplateBinding Background}" Margin="{TemplateBinding Padding}" />
                                        <local:FontIconFallback Foreground="{TemplateBinding Foreground}" FontFamily="{DynamicResource SymbolThemeFontFamily}" FontSize="12" Data="{StaticResource CalendarDatePickerTopHeaderMargin}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Grid>
                                </ControlTemplate>
                            </Grid.Resources>
                            <Border Name="Background" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{TemplateBinding CornerRadius}" MinHeight="{DynamicResource ComboBoxMinHeight}" />
                            <DatePickerTextBox Name="PART_TextBox" BorderThickness="{TemplateBinding BorderThickness}" Padding="{DynamicResource ComboBoxEditableTextPadding}" HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" Focusable="{TemplateBinding Focusable}" CornerRadius="{TemplateBinding CornerRadius}" />
                            <Button Name="PART_Button" Focusable="False" Background="Transparent" Foreground="{TemplateBinding Foreground}" Width="30" Padding="0,1,1,1" HorizontalAlignment="Right" VerticalAlignment="Stretch" Template="{StaticResource Calendar}" />
                            <Popup Name="PART_Popup" AllowsTransparency="True" Placement="Bottom" StaysOpen="False">
                                <Popup.PlacementRectangle>
                                    <MultiBinding Converter="{primitives:PlacementRectangleConverter Margin=0,-4}">
                                        <Binding ElementName="PART_Root" Path="ActualWidth" />
                                        <Binding ElementName="PART_Root" Path="ActualHeight" />
                                    </MultiBinding>
                                </Popup.PlacementRectangle>
                            </Popup>
                        </Grid>
                        <ContentPresenter Name="DescriptionPresenter" Row="2" Content="{TemplateBinding Description}" Visibility="{TemplateBinding DescriptionVisibility}" Foreground="{DynamicResource SystemControlDescriptionTextForegroundBrush}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="{x:Type DatePicker}" TargetType="{x:Type DatePicker}" BasedOn="{StaticResource DefaultDatePickerTextBoxStyle}" />
</ResourceDictionary>

