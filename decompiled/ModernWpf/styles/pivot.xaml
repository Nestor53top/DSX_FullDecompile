<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:ModernWpf.Controls" xmlns:primitives="clr-namespace:ModernWpf.Controls.Primitives">
    <primitives:CornerRadiusFilterConverter x:Key="TopLeftCornerRadiusDoubleValueConverter" Filter="TopLeftValue" />
    <primitives:CornerRadiusFilterConverter x:Key="BottomRightCornerRadiusDoubleValueConverter" Filter="BottomRightValue" />
    <Style x:Key="TabItemPivotStyle" TargetType="{x:Type TabItem}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Background" Value="{DynamicResource PivotHeaderItemBackgroundUnselected}" />
        <Setter Property="Padding" Value="{DynamicResource PivotHeaderItemMargin}" />
        <Setter Property="Height" Value="48" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static FocusVisualStyle}}" />
        <Setter Property="UseSystemFocusVisuals" Value="{DynamicResource UseSystemFocusVisuals}" />
        <Setter Property="CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabItem}">
                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="False" />
                                <Condition SourceName="Border" Property="IsMouseOver" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="SelectedPipe" Property="Visibility" Value="Collapsed" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True" />
                                <Condition SourceName="Border" Property="IsMouseOver" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter Property="ZIndex" Value="1" />
                            <Setter TargetName="ContentPresenter" Value="{DynamicResource PivotHeaderItemForegroundSelected}" Property="Foreground" />
                            <Setter TargetName="Border" Value="{DynamicResource PivotHeaderItemBackgroundSelected}" Property="Background" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="False" />
                                <Condition SourceName="Border" Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentPresenter" Value="{DynamicResource PivotHeaderItemForegroundUnselectedPointerOver}" Property="Foreground" />
                            <Setter TargetName="Border" Value="{DynamicResource PivotHeaderItemBackgroundUnselectedPointerOver}" Property="Background" />
                            <Setter TargetName="SelectedPipe" Property="Visibility" Value="Collapsed" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True" />
                                <Condition SourceName="Border" Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter Property="ZIndex" Value="1" />
                            <Setter TargetName="ContentPresenter" Value="{DynamicResource PivotHeaderItemForegroundSelectedPointerOver}" Property="Foreground" />
                            <Setter TargetName="Border" Value="{DynamicResource PivotHeaderItemBackgroundSelectedPointerOver}" Property="Background" />
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ContentPresenter" Value="{DynamicResource PivotHeaderItemForegroundDisabled}" Property="Foreground" />
                            <Setter TargetName="Border" Value="{DynamicResource PivotHeaderItemBackgroundDisabled}" Property="Background" />
                            <Setter TargetName="SelectedPipe" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                    <Border Name="Border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
                        <Grid>
                            <ContentPresenter Name="ContentPresenter" ContentSource="Header" Foreground="{DynamicResource PivotHeaderItemForegroundUnselected}" FontSize="{DynamicResource PivotHeaderItemFontSize}" FontFamily="{DynamicResource PivotHeaderItemFontFamily}" FontWeight="{DynamicResource PivotHeaderItemThemeFontWeight}" VerticalAlignment="Center" HorizontalAlignment="Stretch" Focusable="False" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            <Rectangle Name="SelectedPipe" Fill="{DynamicResource PivotHeaderItemSelectedPipeFill}" Height="2" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Margin="0,0,0,2" RadiusX="{TemplateBinding primitives:ControlHelper.CornerRadius , Converter={StaticResource TopLeftCornerRadiusDoubleValueConverter}}" RadiusY="{TemplateBinding primitives:ControlHelper.CornerRadius , Converter={StaticResource BottomRightCornerRadiusDoubleValueConverter}}" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="TabControlPivotStyle" TargetType="{x:Type TabControl}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Background" Value="{DynamicResource PivotBackground}" />
        <Setter Property="FontFamily" Value="{DynamicResource ContentControlThemeFontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource TopLeftCornerRadiusDoubleValueConverter}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabControl}">
                    <ControlTemplate.Resources>
                        <StreamGeometry Resources="ChevronLeft">M 14.091797 19.970703 L 4.111328 10 L 14.091797 0.029297 L 14.970703 0.908203 L 5.888672 10 L 14.970703 19.091797 Z</StreamGeometry>
                        <StreamGeometry Resources="ChevronRight">M 5.029297 19.091797 L 14.111328 10 L 5.029297 0.908203 L 5.908203 0.029297 L 15.888672 10 L 5.908203 19.970703 Z</StreamGeometry>
                    </ControlTemplate.Resources>
                    <ControlTemplate.Triggers>
                        <Trigger SourceName="TitleContentControl" Property="HasContent" Value="True">
                            <Setter TargetName="TitleContentControl" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition SourceName="headerPanel" Property="IsMouseOver" Value="True" />
                                <Condition SourceName="contentPanel" Property="IsMouseOver" Value="False" />
                                <Condition SourceName="ScrollViewer" Property="CanScrollLeft" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PreviousButton" Property="Opacity" Value="1" />
                            <Setter TargetName="PreviousButton" Property="IsEnabled" Value="True" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition SourceName="headerPanel" Property="IsMouseOver" Value="True" />
                                <Condition SourceName="contentPanel" Property="IsMouseOver" Value="False" />
                                <Condition SourceName="ScrollViewer" Property="CanScrollRight" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="NextButton" Property="Opacity" Value="1" />
                            <Setter TargetName="NextButton" Property="IsEnabled" Value="True" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                    <Grid Name="templateRoot" Background="{TemplateBinding Background}" TabNavigation="Local" SnapsToDevicePixels="true" ClipToBounds="true">
                        <Grid.Resources>
                            <Style Resources="BaseContentControlStyle" TargetType="{x:Type ContentControl}">
                                <Setter Property="HorizontalContentAlignment" Value="Left" />
                                <Setter Property="VerticalContentAlignment" Value="Top" />
                                <Setter Property="FontFamily" Value="{DynamicResource {x:Static MessageFontFamily}}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type ContentControl}">
                                            <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" Margin="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                            <Style Resources="TitleContentControlStyle" TargetType="{x:Type ContentControl}" BasedOn="{StaticResource BottomRightCornerRadiusDoubleValueConverter}">
                                <Setter Property="FontFamily" Value="{DynamicResource PivotTitleFontFamily}" />
                                <Setter Property="FontWeight" Value="{DynamicResource PivotTitleThemeFontWeight}" />
                                <Setter Property="FontSize" Value="{DynamicResource PivotTitleFontSize}" />
                            </Style>
                        </Grid.Resources>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <ContentControl Name="TitleContentControl" IsTabStop="False" Margin="{DynamicResource PivotPortraitThemePadding}" Style="{StaticResource TabItemPivotStyle}" Visibility="Collapsed" Content="{TemplateBinding Title}" ContentTemplate="{TemplateBinding TitleTemplate}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        <Grid Row="1" Margin="{TemplateBinding Padding}">
                            <Grid.Resources>
                                <ControlTemplate Resources="NextTemplate" TargetType="{x:Type ButtonBase}">
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Root" Value="{DynamicResource PivotNextButtonBackgroundPointerOver}" Property="Background" />
                                            <Setter TargetName="Root" Value="{DynamicResource PivotNextButtonBorderBrushPointerOver}" Property="BorderBrush" />
                                            <Setter TargetName="Arrow" Value="{DynamicResource PivotNextButtonForegroundPointerOver}" Property="Foreground" />
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="Root" Value="{DynamicResource PivotNextButtonBackgroundPressed}" Property="Background" />
                                            <Setter TargetName="Root" Value="{DynamicResource PivotNextButtonBorderBrushPressed}" Property="BorderBrush" />
                                            <Setter TargetName="Arrow" Value="{DynamicResource PivotNextButtonForegroundPressed}" Property="Foreground" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                    <Border Name="Root" Background="{DynamicResource PivotNextButtonBackground}" BorderThickness="{DynamicResource PivotNavButtonBorderThemeThickness}" BorderBrush="{DynamicResource PivotNextButtonBorderBrush}">
                                        <local:FontIconFallback Name="Arrow" FontFamily="{DynamicResource SymbolThemeFontFamily}" FontSize="12" Foreground="{DynamicResource PivotNextButtonForeground}" Data="{StaticResource BaseContentControlStyle}" HorizontalAlignment="Center" VerticalAlignment="Center" UseLayoutRounding="False" />
                                    </Border>
                                </ControlTemplate>
                                <ControlTemplate Resources="PreviousTemplate" TargetType="{x:Type ButtonBase}">
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Root" Value="{DynamicResource PivotPreviousButtonBackgroundPointerOver}" Property="Background" />
                                            <Setter TargetName="Root" Value="{DynamicResource PivotPreviousButtonBorderBrushPointerOver}" Property="BorderBrush" />
                                            <Setter TargetName="Arrow" Value="{DynamicResource PivotPreviousButtonForegroundPointerOver}" Property="Foreground" />
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="Root" Value="{DynamicResource PivotPreviousButtonBackgroundPressed}" Property="Background" />
                                            <Setter TargetName="Root" Value="{DynamicResource PivotPreviousButtonBorderBrushPressed}" Property="BorderBrush" />
                                            <Setter TargetName="Arrow" Value="{DynamicResource PivotPreviousButtonForegroundPressed}" Property="Foreground" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                    <Border Name="Root" Background="{DynamicResource PivotPreviousButtonBackground}" BorderThickness="{DynamicResource PivotNavButtonBorderThemeThickness}" BorderBrush="{DynamicResource PivotPreviousButtonBorderBrush}">
                                        <local:FontIconFallback Name="Arrow" FontFamily="{DynamicResource SymbolThemeFontFamily}" FontSize="12" Foreground="{DynamicResource PivotPreviousButtonForeground}" Data="{StaticResource TitleContentControlStyle}" HorizontalAlignment="Center" VerticalAlignment="Center" UseLayoutRounding="False" />
                                    </Border>
                                </ControlTemplate>
                            </Grid.Resources>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid Name="headerPanel" Row="0" Background="{TemplateBinding Background}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Name="LeftHeaderPresenter" Content="{TemplateBinding LeftHeader}" ContentTemplate="{TemplateBinding LeftHeaderTemplate}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                <primitives:PivotHeaderScrollViewer Name="ScrollViewer" Column="1" Style="{DynamicResource {x:Type ScrollViewer}}" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled" VerticalContentAlignment="Stretch" Template="{DynamicResource ScrollViewerScrollBarlessTemplate}">
                                    <StackPanel ZIndex="1" Background="{DynamicResource PivotHeaderBackground}" IsItemsHost="true" Orientation="Horizontal" TabIndex="1" TabNavigation="Once" DirectionalNavigation="Contained" />
                                </primitives:PivotHeaderScrollViewer>
                                <RepeatButton Name="PreviousButton" Column="1" Template="{StaticResource ChevronRight}" Width="20" Height="36" Margin="{DynamicResource PivotNavButtonMargin}" IsTabStop="False" IsEnabled="False" HorizontalAlignment="Left" VerticalAlignment="Top" Opacity="0" Background="Transparent" Command="{Static LineLeftCommand}" FocusVisualStyle="{Null}" CommandTarget="{Binding ElementName=ScrollViewer}" />
                                <RepeatButton Name="NextButton" Column="1" Template="{StaticResource ChevronLeft}" Width="20" Height="36" Margin="{DynamicResource PivotNavButtonMargin}" IsTabStop="False" IsEnabled="False" HorizontalAlignment="Right" VerticalAlignment="Top" Opacity="0" Background="Transparent" Command="{Static LineRightCommand}" FocusVisualStyle="{Null}" CommandTarget="{Binding ElementName=ScrollViewer}" />
                                <ContentPresenter Name="RightHeaderPresenter" Column="2" Content="{TemplateBinding RightHeader}" ContentTemplate="{TemplateBinding RightHeaderTemplate}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Grid>
                            <Border Name="contentPanel" Row="1" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" DirectionalNavigation="Contained" TabIndex="2" TabNavigation="Local">
                                <ContentPresenter Name="PART_SelectedContentHost" ContentSource="SelectedContent" Margin="{DynamicResource PivotItemMargin}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Border>
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>

