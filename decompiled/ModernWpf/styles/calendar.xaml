<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:ModernWpf.Controls" xmlns:primitives="clr-namespace:ModernWpf.Controls.Primitives">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Button.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <Style x:Key="DefaultCalendarItemStyle" TargetType="{x:Type CalendarItem}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CalendarItem}">
                    <ControlTemplate.Resources>
                        <Style TargetType="{x:Type TextBlock}" TargetType="WeekDayNameStyle">
                            <Setter x:Key="{x:Type TextBlock}" Property="FontWeight" Value="Normal" />
                            <Setter Property="FontSize" Value="12" />
                            <Setter Property="TextTrimming" Value="None" />
                            <Setter Property="TextWrapping" Value="Wrap" />
                            <Setter Property="LineStackingStrategy" Value="MaxHeight" />
                            <Setter Property="HorizontalAlignment" Value="Center" />
                            <Setter Property="VerticalAlignment" Value="Center" />
                        </Style>
                        <DataTemplate>
                            <TextBlock x:Key="{Static CalendarItem.DayTitleTemplateResourceKey}" Style="{StaticResource WeekDayNameStyle}" Text="{Binding}" />
                        </DataTemplate>
                        <StreamGeometry TargetType="ChevronDown">M 18.935547 4.560547 L 19.814453 5.439453 L 10 15.253906 L 0.185547 5.439453 L 1.064453 4.560547 L 10 13.496094 Z</StreamGeometry>
                        <StreamGeometry TargetType="ChevronUp">M 19.091797 14.970703 L 10 5.888672 L 0.908203 14.970703 L 0.029297 14.091797 L 10 4.111328 L 19.970703 14.091797 Z</StreamGeometry>
                        <DataTemplate TargetType="IconTemplate">
                            <local:FontIconFallback Data="{Binding}" />
                        </DataTemplate>
                    </ControlTemplate.Resources>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="PART_MonthView" Value="{DynamicResource CalendarViewWeekDayForegroundDisabled}" Property="Foreground" />
                        </Trigger>
                        <DataTrigger Value="Year" Binding="{Binding DisplayMode , RelativeSource={RelativeSource FindAncestor , AncestorType={x:Type Calendar}}}">
                            <Setter TargetName="PART_MonthView" Property="Visibility" Value="Hidden" />
                            <Setter TargetName="PART_YearView" Property="Visibility" Value="Visible" />
                            <Setter TargetName="BackgroundLayer" Property="Margin" Value="0" />
                        </DataTrigger>
                        <DataTrigger Value="Decade" Binding="{Binding DisplayMode , RelativeSource={RelativeSource FindAncestor , AncestorType={x:Type Calendar}}}">
                            <Setter TargetName="PART_MonthView" Property="Visibility" Value="Hidden" />
                            <Setter TargetName="PART_YearView" Property="Visibility" Value="Visible" />
                            <Setter TargetName="BackgroundLayer" Property="Margin" Value="0" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                    <Grid Name="PART_Root" SnapsToDevicePixels="True">
                        <Grid.Resources>
                            <Style TargetType="{x:Type Button}" TargetType="NavigationButtonStyle" BasedOn="{StaticResource DefaultButtonStyle}">
                                <Setter x:Key="{x:Type Button}" Property="HorizontalAlignment" Value="Stretch" />
                                <Setter Property="VerticalAlignment" Value="Stretch" />
                                <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static FocusVisualStyle}}" />
                                <Setter Property="UseSystemFocusVisuals" Value="{DynamicResource UseSystemFocusVisuals}" />
                                <Setter Property="FontSize" Value="20" />
                                <Setter Property="Background" Value="{DynamicResource CalendarViewNavigationButtonBackground}" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type Button}">
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Border" Value="{DynamicResource CalendarViewNavigationButtonBorderBrushPointerOver}" Property="BorderBrush" />
                                                    <Setter TargetName="Text" Value="{DynamicResource CalendarViewNavigationButtonForegroundPointerOver}" Property="Foreground" />
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="Text" Value="{DynamicResource CalendarViewNavigationButtonForegroundPressed}" Property="Foreground" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                            <Border Background="{TemplateBinding Background}" Margin="{TemplateBinding Padding}">
                                                <Border Name="Border" BorderBrush="{DynamicResource CalendarViewNavigationButtonBorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                                    <ContentPresenter Name="Text" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                                </Border>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Grid.Resources>
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{TemplateBinding CornerRadius}">
                            <Grid VerticalAlignment="{TemplateBinding VerticalContentAlignment}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="40" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="5*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Button Name="PART_HeaderButton" Style="{StaticResource NavigationButtonStyle}" Foreground="{TemplateBinding Foreground}" Focusable="False" Padding="12,0,0,0" HorizontalContentAlignment="Left" />
                                    <Button Name="PART_PreviousButton" Column="1" Content="{StaticResource ChevronUp}" ContentTemplate="{StaticResource IconTemplate}" FontFamily="{DynamicResource SymbolThemeFontFamily}" Focusable="False" Padding="1" Foreground="{TemplateBinding Foreground}" HorizontalContentAlignment="Center" Style="{StaticResource NavigationButtonStyle}" />
                                    <Button Name="PART_NextButton" Column="2" Content="{StaticResource ChevronDown}" ContentTemplate="{StaticResource IconTemplate}" FontFamily="{DynamicResource SymbolThemeFontFamily}" Focusable="False" Padding="1" Foreground="{TemplateBinding Foreground}" HorizontalContentAlignment="Center" Style="{StaticResource NavigationButtonStyle}" />
                                </Grid>
                                <Grid Row="1">
                                    <Rectangle Name="BackgroundLayer" Fill="{TemplateBinding BorderBrush}" Margin="0,38,0,0" />
                                    <Grid Name="PART_MonthView" Visibility="Visible" Foreground="{DynamicResource CalendarViewCalendarItemForeground}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="38" />
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>
                                    </Grid>
                                    <Grid Name="PART_YearView" Visibility="Hidden">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>
                                    </Grid>
                                </Grid>
                            </Grid>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="{x:Type CalendarItem}" TargetType="{x:Type CalendarItem}" BasedOn="{StaticResource WeekDayNameStyle}" />
    <Style x:Key="DefaultCalendarDayButtonStyle" TargetType="{x:Type CalendarDayButton}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Background" Value="{DynamicResource CalendarViewCalendarItemRevealBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource CalendarViewCalendarItemForeground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CalendarViewCalendarItemRevealBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="MinWidth" Value="40" />
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="Margin" Value="1" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static FocusVisualStyle}}" />
        <Setter Property="UseSystemFocusVisuals" Value="True" />
        <Setter Property="FocusVisualMargin" Value="-2" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CalendarDayButton}">
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HighlightBorder" Value="{DynamicResource CalendarViewHoverBorderBrush}" Property="BorderBrush" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="HighlightBorder" Value="{DynamicResource CalendarViewPressedBorderBrush}" Property="BorderBrush" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="HighlightBorder" Value="{DynamicResource CalendarViewSelectedBorderBrush}" Property="BorderBrush" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="HighlightBorder" Value="{DynamicResource CalendarViewSelectedHoverBorderBrush}" Property="BorderBrush" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True" />
                                <Condition Property="IsPressed" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="HighlightBorder" Value="{DynamicResource CalendarViewSelectedPressedBorderBrush}" Property="BorderBrush" />
                        </MultiTrigger>
                        <Trigger Property="IsInactive" Value="True">
                            <Setter TargetName="Background" Value="{DynamicResource SystemControlDisabledChromeMediumLowBrush}" Property="Fill" />
                        </Trigger>
                        <Trigger SourceName="DayStatesListener" Property="CurrentStateName" Value="Today">
                            <Setter TargetName="Background" Value="{DynamicResource SystemControlHighlightAccentBrush}" Property="Fill" />
                            <Setter TargetName="NormalText" Value="{DynamicResource CalendarViewTodayForeground}" Property="Foreground" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsBlackedOut" Value="True" />
                                <Condition SourceName="DayStatesListener" Property="CurrentStateName" Value="RegularDay" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="NormalText" Value="{DynamicResource CalendarViewBlackoutForeground}" Property="Foreground" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsBlackedOut" Value="True" />
                                <Condition SourceName="DayStatesListener" Property="CurrentStateName" Value="Today" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Background" Property="Opacity" Value="0.5" />
                            <Setter TargetName="NormalText" Property="Opacity" Value="0.4" />
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="NormalText" Value="{DynamicResource CalendarViewBlackoutForeground}" Property="Foreground" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                    <Grid SnapsToDevicePixels="True">
                        <Grid.VisualStateGroups>
                            <VisualStateGroup x:Name="DayStates" Listener="{primitives:VisualStateGroupListener Name=DayStatesListener}">
                                <VisualState x:Name="RegularDay" />
                                <VisualState x:Name="Today" />
                            </VisualStateGroup>
                        </Grid.VisualStateGroups>
                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Margin="-1" />
                        <Rectangle Name="Background" Fill="{TemplateBinding Background}" />
                        <Border Name="HighlightBorder" BorderThickness="2" />
                        <TextBlock Name="NormalText" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" FontWeight="{TemplateBinding FontWeight}" Text="{TemplateBinding Content}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" RenderTransform="{TranslateTransform Y=-1}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="{x:Type CalendarDayButton}" TargetType="{x:Type CalendarDayButton}" BasedOn="{StaticResource WeekDayNameStyle}" />
    <Style x:Key="DefaultCalendarButtonStyle" TargetType="{x:Type CalendarButton}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Background" Value="{DynamicResource CalendarViewCalendarItemRevealBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource CalendarViewCalendarItemForeground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CalendarViewCalendarItemRevealBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="MinWidth" Value="40" />
        <Setter Property="MinHeight" Value="42" />
        <Setter Property="Margin" Value="1" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static FocusVisualStyle}}" />
        <Setter Property="UseSystemFocusVisuals" Value="True" />
        <Setter Property="FocusVisualMargin" Value="-2" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CalendarButton}">
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HighlightBorder" Value="{DynamicResource CalendarViewHoverBorderBrush}" Property="BorderBrush" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="HighlightBorder" Value="{DynamicResource CalendarViewPressedBorderBrush}" Property="BorderBrush" />
                        </Trigger>
                        <Trigger Property="HasSelectedDays" Value="True">
                            <Setter TargetName="Background" Value="{DynamicResource SystemControlHighlightAccentBrush}" Property="Background" />
                            <Setter TargetName="NormalText" Value="{DynamicResource CalendarViewTodayForeground}" Property="Foreground" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="HasSelectedDays" Value="True" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="HighlightBorder" Value="{DynamicResource CalendarViewSelectedHoverBorderBrush}" Property="BorderBrush" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="HasSelectedDays" Value="True" />
                                <Condition Property="IsPressed" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="HighlightBorder" Value="{DynamicResource CalendarViewSelectedPressedBorderBrush}" Property="BorderBrush" />
                        </MultiTrigger>
                        <Trigger Property="IsInactive" Value="True">
                            <Setter TargetName="Background" Value="{DynamicResource SystemControlDisabledChromeMediumLowBrush}" Property="Background" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="NormalText" Value="{DynamicResource CalendarViewBlackoutForeground}" Property="Foreground" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                    <Grid SnapsToDevicePixels="True">
                        <Border Name="Background" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Margin="-1" />
                        <Border Name="HighlightBorder" BorderThickness="2" />
                        <TextBlock Name="NormalText" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" FontWeight="{TemplateBinding FontWeight}" Text="{TemplateBinding Content}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="{x:Type CalendarButton}" TargetType="{x:Type CalendarButton}" BasedOn="{StaticResource WeekDayNameStyle}" />
    <Style x:Key="DefaultCalendarStyle" TargetType="{x:Type Calendar}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="CalendarItemStyle" Value="{StaticResource WeekDayNameStyle}" />
        <Setter Property="CalendarDayButtonStyle" Value="{StaticResource DefaultButtonStyle}" />
        <Setter Property="CalendarButtonStyle" Value="{StaticResource NavigationButtonStyle}" />
        <Setter Property="Foreground" Value="{DynamicResource CalendarViewForeground}" />
        <Setter Property="Background" Value="{DynamicResource CalendarViewBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CalendarViewBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
        <Setter Property="AutoReleaseMouseCapture" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Calendar}">
                    <StackPanel Name="PART_Root" SnapsToDevicePixels="True">
                        <CalendarItem Name="PART_CalendarItem" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Style="{TemplateBinding CalendarItemStyle}" CornerRadius="{TemplateBinding CornerRadius}" />
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="{x:Type Calendar}" TargetType="{x:Type Calendar}" BasedOn="{StaticResource WeekDayNameStyle}" />
    <Style x:Key="DatePickerCalendarStyle" TargetType="{x:Type Calendar}" BasedOn="{StaticResource WeekDayNameStyle}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Calendar}">
                    <primitives:ThemeShadowChrome Name="Shdw" IsShadowEnabled="{DynamicResource {x:Static DropShadow}}" CornerRadius="{TemplateBinding CornerRadius}" SnapsToDevicePixels="True" Storyboard="{DynamicResource OpeningStoryboard}">
                        <primitives:ThemeShadowChrome.Resources>
                            <Storyboard TargetType="OpeningStoryboard">
                                <DoubleAnimation TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.100" Target="{Binding ElementName=Shdw}" />
                            </Storyboard>
                        </primitives:ThemeShadowChrome.Resources>
                        <StackPanel Name="PART_Root">
                            <CalendarItem Name="PART_CalendarItem" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Style="{TemplateBinding CalendarItemStyle}" CornerRadius="{TemplateBinding CornerRadius}" />
                        </StackPanel>
                    </primitives:ThemeShadowChrome>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>

