<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:ui="http://schemas.modernwpf.com/2019" xmlns:local="clr-namespace:ModernWpf.Controls">
    <Style x:Key="{x:Type local:RatingControl}" TargetType="{x:Type local:RatingControl}">
        <Setter Property="Height" Value="32" />
        <Setter Property="Foreground" Value="{DynamicResource RatingControlCaptionForeground}" />
        <Setter Property="UseSystemFocusVisuals" Value="{DynamicResource UseSystemFocusVisuals}" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static FocusVisualStyle}}" />
        <Setter Property="FontFamily" Value="{DynamicResource SymbolThemeFontFamily}" />
        <Setter Property="ItemInfo" Value="{DynamicResource RatingControlDefaultPathInfo}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:RatingControl}">
                    <ControlTemplate.Triggers>
                        <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="Disabled">
                            <Setter TargetName="ForegroundContentPresenter" Value="{DynamicResource RatingControlDisabledSelectedForeground}" Property="Foreground" />
                        </Trigger>
                        <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="Placeholder">
                            <Setter TargetName="ForegroundContentPresenter" Value="{DynamicResource RatingControlPlaceholderForeground}" Property="Foreground" />
                        </Trigger>
                        <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="PointerOverPlaceholder">
                            <Setter TargetName="ForegroundContentPresenter" Value="{DynamicResource RatingControlPointerOverPlaceholderForeground}" Property="Foreground" />
                        </Trigger>
                        <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="PointerOverUnselected">
                            <Setter TargetName="ForegroundContentPresenter" Value="{DynamicResource RatingControlPointerOverUnselectedForeground}" Property="Foreground" />
                        </Trigger>
                        <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="Set">
                            <Setter TargetName="ForegroundContentPresenter" Value="{DynamicResource RatingControlSelectedForeground}" Property="Foreground" />
                        </Trigger>
                        <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="PointerOverSet">
                            <Setter TargetName="ForegroundContentPresenter" Value="{DynamicResource RatingControlSelectedForeground}" Property="Foreground" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                    <Grid Name="LayoutRoot">
                        <Grid.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates" Listener="{VisualStateGroupListener Name=CommonStatesListener}">
                                <VisualState x:Name="Disabled" />
                                <VisualState x:Name="Placeholder" />
                                <VisualState x:Name="PointerOverPlaceholder" />
                                <VisualState x:Name="PointerOverUnselected" />
                                <VisualState x:Name="Set" />
                                <VisualState x:Name="PointerOverSet" />
                            </VisualStateGroup>
                        </Grid.VisualStateGroups>
                        <StackPanel Orientation="Horizontal" Row="0" Margin="-20,-20,-20,-20">
                            <StackPanel Name="RatingBackgroundStackPanel" Orientation="Horizontal" Background="Transparent" Margin="20,20,0,20" />
                            <TextBlock Name="Caption" Height="32" Margin="4,9,20,0" Style="{DynamicResource CaptionTextBlockStyle}" VerticalAlignment="Center" Name="RatingCaption" IsHitTestVisible="False" Text="{TemplateBinding Caption}" />
                        </StackPanel>
                        <ContentPresenter Name="ForegroundContentPresenter" IsHitTestVisible="False" Row="0">
                            <ContentPresenter.Content>
                                <StackPanel Orientation="Horizontal" Margin="-40,-40,-40,-40">
                                    <StackPanel Name="RatingForegroundStackPanel" Orientation="Horizontal" IsHitTestVisible="False" Margin="40,40,40,40" />
                                </StackPanel>
                            </ContentPresenter.Content>
                        </ContentPresenter>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>

