<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:ui="http://schemas.modernwpf.com/2019" xmlns:sys="clr-namespace:System;assembly=mscorlib" xmlns:local="clr-namespace:ModernWpf.Controls">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/ModernWpf;component/Styles/Button.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <Double x:Key="SplitButtonPrimaryButtonSize">32</Double>
    <Double x:Key="SplitButtonSecondaryButtonSize">32</Double>
    <CornerRadiusFilterConverter x:Key="LeftCornerRadiusFilterConverter" Filter="Left" />
    <CornerRadiusFilterConverter x:Key="RightCornerRadiusFilterConverter" Filter="Right" />
    <Style x:Key="SplitButtonStyle" TargetType="{x:Type local:SplitButton}">
        <Setter Property="Background" Value="{DynamicResource SplitButtonBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource SplitButtonForeground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource SplitButtonBorderBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource SplitButtonBorderThemeThickness}" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="{DynamicResource ContentControlThemeFontFamily}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
        <Setter Property="UseSystemFocusVisuals" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static FocusVisualStyle}}" />
        <Setter Property="FocusVisualMargin" Value="-3" />
        <Setter Property="IsTabStop" Value="True" />
        <Setter Property="Padding" Value="{DynamicResource ButtonPadding}" />
        <Setter Property="CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:SplitButton}">
                    <ControlTemplate.Resources>
                        <StreamGeometry Name="ChevronDown">M 18.935547 4.560547 L 19.814453 5.439453 L 10 15.253906 L 0.185547 5.439453 L 1.064453 4.560547 L 10 13.496094 Z</StreamGeometry>
                    </ControlTemplate.Resources>
                    <ControlTemplate.Triggers>
                        <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="FlyoutOpen">
                            <Setter TargetName="PrimaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundPressed}" Property="Background" />
                            <Setter TargetName="SecondaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundPressed}" Property="Background" />
                            <Setter TargetName="Border" Value="{DynamicResource SplitButtonBorderBrushPressed}" Property="BorderBrush" />
                            <Setter TargetName="PrimaryButton" Value="{DynamicResource SplitButtonForegroundPressed}" Property="Foreground" />
                            <Setter TargetName="SecondaryButton" Value="{DynamicResource SplitButtonForegroundPressed}" Property="Foreground" />
                        </Trigger>
                        <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="TouchPressed">
                            <Setter TargetName="PrimaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundPressed}" Property="Background" />
                            <Setter TargetName="SecondaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundPressed}" Property="Background" />
                            <Setter TargetName="Border" Value="{DynamicResource SplitButtonBorderBrushPressed}" Property="BorderBrush" />
                            <Setter TargetName="PrimaryButton" Value="{DynamicResource SplitButtonForegroundPressed}" Property="Foreground" />
                            <Setter TargetName="SecondaryButton" Value="{DynamicResource SplitButtonForegroundPressed}" Property="Foreground" />
                        </Trigger>
                        <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="PrimaryPointerOver">
                            <Setter TargetName="PrimaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundPointerOver}" Property="Background" />
                            <Setter TargetName="PrimaryButton" Value="{DynamicResource SplitButtonBorderBrushPointerOver}" Property="BorderBrush" />
                            <Setter TargetName="PrimaryButton" Value="{DynamicResource SplitButtonForegroundPointerOver}" Property="Foreground" />
                            <Setter TargetName="SecondaryBackgroundGrid" Value="{DynamicResource SplitButtonBackground}" Property="Background" />
                        </Trigger>
                        <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="PrimaryPressed">
                            <Setter TargetName="PrimaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundPressed}" Property="Background" />
                            <Setter TargetName="PrimaryButton" Value="{DynamicResource SplitButtonBorderBrushPressed}" Property="BorderBrush" />
                            <Setter TargetName="PrimaryButton" Value="{DynamicResource SplitButtonForegroundPressed}" Property="Foreground" />
                            <Setter TargetName="SecondaryBackgroundGrid" Value="{DynamicResource SplitButtonBackground}" Property="Background" />
                        </Trigger>
                        <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="SecondaryPointerOver">
                            <Setter TargetName="PrimaryBackgroundGrid" Value="{DynamicResource SplitButtonBackground}" Property="Background" />
                            <Setter TargetName="SecondaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundPointerOver}" Property="Background" />
                            <Setter TargetName="SecondaryButton" Value="{DynamicResource SplitButtonBorderBrushPointerOver}" Property="BorderBrush" />
                            <Setter TargetName="SecondaryButton" Value="{DynamicResource SplitButtonForegroundPointerOver}" Property="Foreground" />
                        </Trigger>
                        <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="SecondaryPressed">
                            <Setter TargetName="PrimaryBackgroundGrid" Value="{DynamicResource SplitButtonBackground}" Property="Background" />
                            <Setter TargetName="SecondaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundPressed}" Property="Background" />
                            <Setter TargetName="SecondaryButton" Value="{DynamicResource SplitButtonBorderBrushPressed}" Property="BorderBrush" />
                            <Setter TargetName="SecondaryButton" Value="{DynamicResource SplitButtonForegroundPressed}" Property="Foreground" />
                        </Trigger>
                        <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="Checked">
                            <Setter TargetName="PrimaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundChecked}" Property="Background" />
                            <Setter TargetName="SecondaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundChecked}" Property="Background" />
                            <Setter TargetName="Border" Value="{DynamicResource SplitButtonBorderBrushChecked}" Property="BorderBrush" />
                            <Setter TargetName="PrimaryButton" Value="{DynamicResource SplitButtonForegroundChecked}" Property="Foreground" />
                            <Setter TargetName="SecondaryButton" Value="{DynamicResource SplitButtonForegroundChecked}" Property="Foreground" />
                            <Setter Value="{DynamicResource SplitButtonForegroundChecked}" Property="Foreground" />
                        </Trigger>
                        <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="CheckedFlyoutOpen">
                            <Setter TargetName="PrimaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundCheckedPressed}" Property="Background" />
                            <Setter TargetName="SecondaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundCheckedPressed}" Property="Background" />
                            <Setter TargetName="Border" Value="{DynamicResource SplitButtonBorderBrushCheckedPressed}" Property="BorderBrush" />
                            <Setter TargetName="PrimaryButton" Value="{DynamicResource SplitButtonForegroundCheckedPressed}" Property="Foreground" />
                            <Setter TargetName="SecondaryButton" Value="{DynamicResource SplitButtonForegroundCheckedPressed}" Property="Foreground" />
                            <Setter Value="{DynamicResource SplitButtonForegroundCheckedPressed}" Property="Foreground" />
                        </Trigger>
                        <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="CheckedTouchPressed">
                            <Setter TargetName="PrimaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundCheckedPressed}" Property="Background" />
                            <Setter TargetName="SecondaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundCheckedPressed}" Property="Background" />
                            <Setter TargetName="Border" Value="{DynamicResource SplitButtonBorderBrushCheckedPressed}" Property="BorderBrush" />
                            <Setter TargetName="PrimaryButton" Value="{DynamicResource SplitButtonForegroundCheckedPressed}" Property="Foreground" />
                            <Setter TargetName="SecondaryButton" Value="{DynamicResource SplitButtonForegroundCheckedPressed}" Property="Foreground" />
                            <Setter Value="{DynamicResource SplitButtonForegroundCheckedPressed}" Property="Foreground" />
                        </Trigger>
                        <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="CheckedPrimaryPointerOver">
                            <Setter TargetName="Border" Value="{DynamicResource SplitButtonBorderBrushChecked}" Property="BorderBrush" />
                            <Setter TargetName="PrimaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundCheckedPointerOver}" Property="Background" />
                            <Setter TargetName="PrimaryButton" Value="{DynamicResource SplitButtonBorderBrushCheckedPointerOver}" Property="BorderBrush" />
                            <Setter TargetName="PrimaryButton" Value="{DynamicResource SplitButtonForegroundCheckedPointerOver}" Property="Foreground" />
                            <Setter TargetName="SecondaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundChecked}" Property="Background" />
                            <Setter TargetName="SecondaryButton" Value="{DynamicResource SplitButtonForegroundChecked}" Property="Foreground" />
                            <Setter Value="{DynamicResource SplitButtonForegroundChecked}" Property="Foreground" />
                        </Trigger>
                        <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="CheckedPrimaryPressed">
                            <Setter TargetName="Border" Value="{DynamicResource SplitButtonBorderBrushChecked}" Property="BorderBrush" />
                            <Setter TargetName="PrimaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundCheckedPressed}" Property="Background" />
                            <Setter TargetName="PrimaryButton" Value="{DynamicResource SplitButtonBorderBrushCheckedPressed}" Property="BorderBrush" />
                            <Setter TargetName="PrimaryButton" Value="{DynamicResource SplitButtonForegroundCheckedPressed}" Property="Foreground" />
                            <Setter TargetName="SecondaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundChecked}" Property="Background" />
                            <Setter TargetName="SecondaryButton" Value="{DynamicResource SplitButtonForegroundChecked}" Property="Foreground" />
                            <Setter Value="{DynamicResource SplitButtonForegroundChecked}" Property="Foreground" />
                        </Trigger>
                        <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="CheckedSecondaryPointerOver">
                            <Setter TargetName="Border" Value="{DynamicResource SplitButtonBorderBrushChecked}" Property="BorderBrush" />
                            <Setter TargetName="PrimaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundChecked}" Property="Background" />
                            <Setter TargetName="PrimaryButton" Value="{DynamicResource SplitButtonForegroundChecked}" Property="Foreground" />
                            <Setter TargetName="SecondaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundCheckedPointerOver}" Property="Background" />
                            <Setter TargetName="SecondaryButton" Value="{DynamicResource SplitButtonBorderBrushCheckedPointerOver}" Property="BorderBrush" />
                            <Setter TargetName="SecondaryButton" Value="{DynamicResource SplitButtonForegroundCheckedPointerOver}" Property="Foreground" />
                            <Setter Value="{DynamicResource SplitButtonForegroundChecked}" Property="Foreground" />
                        </Trigger>
                        <Trigger SourceName="CommonStatesListener" Property="CurrentStateName" Value="CheckedSecondaryPressed">
                            <Setter TargetName="Border" Value="{DynamicResource SplitButtonBorderBrushChecked}" Property="BorderBrush" />
                            <Setter TargetName="PrimaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundChecked}" Property="Background" />
                            <Setter TargetName="PrimaryButton" Value="{DynamicResource SplitButtonForegroundChecked}" Property="Foreground" />
                            <Setter TargetName="SecondaryBackgroundGrid" Value="{DynamicResource SplitButtonBackgroundCheckedPressed}" Property="Background" />
                            <Setter TargetName="SecondaryButton" Value="{DynamicResource SplitButtonBorderBrushCheckedPressed}" Property="BorderBrush" />
                            <Setter TargetName="SecondaryButton" Value="{DynamicResource SplitButtonForegroundCheckedPressed}" Property="Foreground" />
                            <Setter Value="{DynamicResource SplitButtonForegroundChecked}" Property="Foreground" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                    <Grid Name="RootGrid" Background="Transparent" SnapsToDevicePixels="True">
                        <Grid.Resources>
                            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource DefaultButtonStyle}">
                                <Setter x:Key="{x:Type Button}" Property="Foreground" Value="{DynamicResource SplitButtonForeground}" />
                                <Setter Property="BorderBrush" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="{DynamicResource SplitButtonBorderThemeThickness}" />
                                <Setter Property="HorizontalAlignment" Value="Left" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Setter Property="FontFamily" Value="{DynamicResource ContentControlThemeFontFamily}" />
                                <Setter Property="FontWeight" Value="Normal" />
                                <Setter Property="FontSize" Value="{DynamicResource ControlContentThemeFontSize}" />
                                <Setter Property="FocusVisualStyle" Value="{DynamicResource {x:Static FocusVisualStyle}}" />
                                <Setter Property="UseSystemFocusVisuals" Value="{DynamicResource UseSystemFocusVisuals}" />
                                <Setter Property="FocusVisualMargin" Value="-3" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type Button}">
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter TargetName="ContentPresenter" Value="{DynamicResource SplitButtonForegroundDisabled}" Property="Foreground" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                            <Grid Name="RootGrid" Background="Transparent" SnapsToDevicePixels="True">
                                                <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                                                    <ContentPresenter Name="ContentPresenter" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Focusable="False" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                                </Border>
                                            </Grid>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Grid.Resources>
                        <Grid.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates" Listener="{VisualStateGroupListener Name=CommonStatesListener}">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="FlyoutOpen" />
                                <VisualState x:Name="TouchPressed" />
                                <VisualState x:Name="PrimaryPointerOver" />
                                <VisualState x:Name="PrimaryPressed" />
                                <VisualState x:Name="SecondaryPointerOver" />
                                <VisualState x:Name="SecondaryPressed" />
                                <VisualState x:Name="Checked" />
                                <VisualState x:Name="CheckedFlyoutOpen" />
                                <VisualState x:Name="CheckedTouchPressed" />
                                <VisualState x:Name="CheckedPrimaryPointerOver" />
                                <VisualState x:Name="CheckedPrimaryPressed" />
                                <VisualState x:Name="CheckedSecondaryPointerOver" />
                                <VisualState x:Name="CheckedSecondaryPressed" />
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="SecondaryButtonPlacementStates">
                                <VisualState x:Name="SecondaryButtonRight" />
                                <VisualState x:Name="SecondaryButtonSpan">
                                    <VisualState.Storyboard>
                                        <Storyboard>
                                            <Int32AnimationUsingKeyFrames TargetName="SecondaryButton" TargetProperty="(Grid.Column)">
                                                <DiscreteInt32KeyFrame KeyTime="0" Value="0" />
                                            </Int32AnimationUsingKeyFrames>
                                            <Int32AnimationUsingKeyFrames TargetName="SecondaryButton" TargetProperty="(Grid.ColumnSpan)">
                                                <DiscreteInt32KeyFrame KeyTime="0" Value="3" />
                                            </Int32AnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState.Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </Grid.VisualStateGroups>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Name="PrimaryButtonColumn" Width="*" MinWidth="{StaticResource SplitButtonPrimaryButtonSize}" />
                            <ColumnDefinition Name="Separator" Width="1" />
                            <ColumnDefinition Name="SecondaryButtonColumn" PixelWidth="{StaticResource SplitButtonSecondaryButtonSize}" />
                        </Grid.ColumnDefinitions>
                        <Border Name="PrimaryBackgroundGrid" Background="{TemplateBinding Background}" CornerRadius="{TemplateBinding CornerRadius , Converter={StaticResource LeftCornerRadiusFilterConverter}}" />
                        <Border Name="SecondaryBackgroundGrid" Background="{TemplateBinding Background}" Column="2" CornerRadius="{TemplateBinding CornerRadius , Converter={StaticResource RightCornerRadiusFilterConverter}}" />
                        <Border Name="Border" ColumnSpan="3" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{TemplateBinding CornerRadius}" />
                        <Button Name="PrimaryButton" Column="0" Foreground="{TemplateBinding Foreground}" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="Transparent" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" Command="{TemplateBinding Command}" CommandParameter="{TemplateBinding CommandParameter}" CommandTarget="{TemplateBinding CommandTarget}" FontFamily="{TemplateBinding FontFamily}" FontSize="{TemplateBinding FontSize}" FontWeight="{TemplateBinding FontWeight}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" Padding="{TemplateBinding Padding}" Focusable="False" />
                        <Button Name="SecondaryButton" Column="2" Foreground="{TemplateBinding Foreground}" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="Transparent" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Padding="0,0,9,0" Focusable="False" Content="{FontIconFallback FontFamily={DynamicResource SymbolThemeFontFamily}, FontSize=12, Data={StaticResource ChevronDown}, VerticalAlignment=Center, HorizontalAlignment=Right}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="{x:Type local:SplitButton}" TargetType="{x:Type local:SplitButton}" BasedOn="{StaticResource DefaultButtonStyle}" />
    <Style x:Key="{x:Type local:ToggleSplitButton}" TargetType="{x:Type local:ToggleSplitButton}" BasedOn="{StaticResource DefaultButtonStyle}" />
</ResourceDictionary>

